protected void init(Map<String, Object> params) {
    ...
    Timer t = getTimer("countTimer");
    t.addTimerListener(new Timer.TimerListener() {
        public void onTimer(Timer timer) {
            //код обработки запуска таймера
        }

        public void onStopTimer(Timer timer) {
            //код обработки остановки таймера
        }
    });
    ...
}