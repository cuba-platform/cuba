@Override
public void init(Map<String, Object> params) {
   //Установка действия
   withoutActionButton.setAction(
      new AbstractAction("someAction") {
         @Override
         public void actionPerform(Component component) {
            showNotification("Some action is done", 
                NotificationType.HUMANIZED);
         }
   );
   //Установка названия
   withoutActionButton.setCaption(getMessage("someAction"));
}