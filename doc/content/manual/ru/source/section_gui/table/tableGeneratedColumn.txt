ordersTable.addGeneratedColumn(
    "product",
    new Table.ColumnGenerator<Order>() {
        @Override
        public Component generateCell(Order entity) {
            LookupField lookupField = componentsFactory.createComponent(LookupField.NAME);
            lookupField.setOptionsDatasource(productsDs);
            lookupField.setWidth("100px");
            return lookupField;
        }
    }
);